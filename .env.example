# Server configuration
# The host interface to bind the HTTP server (use 0.0.0.0 to listen on all interfaces)
HOST=localhost

# The port where the HTTP server listens
PORT=8080

# Database configuration
# For sqlite, this is a file path. It will be created if it does not exist.
# Example values:
#   DATABASE_URL=data/app.db
#   DATABASE_URL=:memory:
#   DATABASE_URL=/absolute/path/app.db
DATABASE_URL=data/app.db
